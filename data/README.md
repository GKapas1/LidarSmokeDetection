# Data

The repository tracks bag metadata (`metadata.yaml`), dataset schemas, window manifests, and QC summaries. Raw recordings (`.mcap`, `.db3`) and generated arrays/previews (`.npz`, `.ply`) are excluded by `.gitignore`.
